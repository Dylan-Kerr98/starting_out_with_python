# If you want to change the size of the graphics window you can use the turtle.set(width, height), the width &
# height are passed as an argument
import turtle

turtle.setup(600, 900)
turtle.forward(45)
turtle.done()
