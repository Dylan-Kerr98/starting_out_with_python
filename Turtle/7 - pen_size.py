# You are able to change the turtle pensize using the turtle.pensize(width), the size is passed as an argument

import turtle

turtle.pensize(15)    # Pensize now width of 15
turtle.circle(200)

turtle.done()
